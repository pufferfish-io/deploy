name: "🚀 Deploy MinIO (cpuv1) + TLS"

on:
  workflow_dispatch:  # только вручную

permissions:
  contents: read
  id-token: write     # (норм ок оставить; secrets здесь НЕ указываются)

jobs:
  deploy-minio:
    runs-on: self-hosted
    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml
      MINIO_IMAGE: minio/minio:RELEASE.2021-04-22T15-44-28Z
      # GitHub сам подставит: репозиторные секреты имеют приоритет над организационными
      MINIO_ROOT_USER: ${{ secrets.MINIO_ROOT_USER }}
      MINIO_ROOT_PASSWORD: ${{ secrets.MINIO_ROOT_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - name: ✅ Check secrets
        shell: bash
        run: |
          set -euo pipefail
          test -n "${MINIO_ROOT_USER:-}" || { echo "❌ MINIO_ROOT_USER missing"; exit 1; }
          test -n "${MINIO_ROOT_PASSWORD:-}" || { echo "❌ MINIO_ROOT_PASSWORD missing"; exit 1; }
          echo "✅ Secrets are available"

      - name: 🛠 Ensure namespace
        run: kubectl get ns minio >/dev/null 2>&1 || kubectl create ns minio

      - name: 🔐 Create secret
        run: |
          kubectl -n minio delete secret minio-root --ignore-not-found
          kubectl -n minio create secret generic minio-root \
            --from-literal=MINIO_ROOT_USER="$MINIO_ROOT_USER" \
            --from-literal=MINIO_ROOT_PASSWORD="$MINIO_ROOT_PASSWORD"

      - name: 📦 Apply manifests
        run: |
          kubectl apply -f k8s/minio/namespace.yaml
          kubectl apply -f k8s/minio/pvc.yaml
          kubectl apply -f k8s/minio/deploy.yaml
          kubectl apply -f k8s/minio/svc.yaml
          kubectl apply -f k8s/minio/ingress.yaml

      - name: ⏳ Wait rollout (with debug on failure)
        shell: bash
        run: |
          set -e
          if ! kubectl -n minio rollout status deploy/minio --timeout=300s; then
            echo "---- pods";        kubectl -n minio get pods -o wide || true
            echo "---- describe";    kubectl -n minio describe deploy/minio || true
            echo "---- logs";        kubectl -n minio logs deploy/minio --tail=200 || true
            exit 1
          fi