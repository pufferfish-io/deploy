name: 🚀 Deploy message-responder-ocr

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (defaults to latest)"
        required: false
        default: "latest"

jobs:
  deploy:
    name: 🔄 Deploy via kubectl
    runs-on: self-hosted

    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v4

      - name: 🔧 Ensure namespace
        run: |
          kubectl get ns app || kubectl create ns app

      - name: 🔐 Apply/update env Secret
        env:
          BOOTSTRAP: ${{ secrets.MSG_RESP_OCR_KAFKA_BOOTSTRAP_SERVERS_VALUE }}
          GROUP_ID: ${{ secrets.MSG_RESP_OCR_KAFKA_GROUP_ID }}
          REQ_TOPIC: ${{ secrets.MSG_RESP_OCR_KAFKA_REQUEST_TOPIC_NAME }}
          RESP_TOPIC: ${{ secrets.MSG_RESP_OCR_KAFKA_RESPONSE_TOPIC_NAME }}
          SASL_USER: ${{ secrets.MSG_RESP_OCR_KAFKA_SASL_USERNAME }}
          SASL_PASS: ${{ secrets.MSG_RESP_OCR_KAFKA_SASL_PASSWORD }}
          CLIENT_ID: ${{ secrets.MSG_RESP_OCR_KAFKA_CLIENT_ID }}
          DOC3TEXT_TOKEN_URL: ${{ secrets.MSG_RESP_OCR_DOC3TEXT_ACCESS_TOKEN_URL }}
          DOC3TEXT_CLIENT_ID: ${{ secrets.MSG_RESP_OCR_DOC3TEXT_CLIENT_ID }}
          DOC3TEXT_CLIENT_SECRET: ${{ secrets.MSG_RESP_OCR_DOC3TEXT_CLIENT_SECRET }}
          DOC3TEXT_G_RPC_URL: ${{ secrets.MSG_RESP_OCR_DOC3TEXT_G_RPC_URL }}
        run: |
          kubectl -n app create secret generic message-responder-ocr-env \
            --from-literal=MSG_RESP_OCR_KAFKA_BOOTSTRAP_SERVERS_VALUE="${BOOTSTRAP}" \
            --from-literal=MSG_RESP_OCR_KAFKA_GROUP_ID="${GROUP_ID}" \
            --from-literal=MSG_RESP_OCR_KAFKA_REQUEST_TOPIC_NAME="${REQ_TOPIC}" \
            --from-literal=MSG_RESP_OCR_KAFKA_RESPONSE_TOPIC_NAME="${RESP_TOPIC}" \
            --from-literal=MSG_RESP_OCR_KAFKA_SASL_USERNAME="${SASL_USER}" \
            --from-literal=MSG_RESP_OCR_KAFKA_SASL_PASSWORD="${SASL_PASS}" \
            --from-literal=MSG_RESP_OCR_KAFKA_CLIENT_ID="${CLIENT_ID}" \
            --from-literal=MSG_RESP_OCR_DOC3TEXT_ACCESS_TOKEN_URL="${DOC3TEXT_TOKEN_URL}" \
            --from-literal=MSG_RESP_OCR_DOC3TEXT_CLIENT_ID="${DOC3TEXT_CLIENT_ID}" \
            --from-literal=MSG_RESP_OCR_DOC3TEXT_CLIENT_SECRET="${DOC3TEXT_CLIENT_SECRET}" \
            --from-literal=MSG_RESP_OCR_DOC3TEXT_G_RPC_URL="${DOC3TEXT_G_RPC_URL}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: 📤 Apply manifests
        run: |
          kubectl -n app apply -f k8s/message-responder-ocr/deploy.yaml

      - name: 🖼 Update image tag
        env:
          IMAGE_TAG: ${{ github.event.inputs.image_tag }}
        run: |
          TAG=${IMAGE_TAG:-latest}
          REPO=ghcr.io/pufferfish-io/message-responder-ocr
          IMAGE="${REPO}:${TAG}"
          echo "Deploying image: ${IMAGE}"
          kubectl -n app set image deployment/message-responder-ocr message-responder-ocr=${IMAGE}

      - name: 🔎 Rollout status
        run: |
          set -e
          kubectl -n app rollout status deployment/message-responder-ocr --timeout=180s
          kubectl -n app get pods -o wide

      - name: 📝 Recent logs (best effort)
        continue-on-error: true
        run: |
          kubectl -n app logs deploy/message-responder-ocr --tail=200
