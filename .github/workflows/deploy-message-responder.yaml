name: 🚀 Deploy message-responder

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (defaults to latest)"
        required: false
        default: "latest"

jobs:
  deploy:
    name: 🔄 Deploy via kubectl
    runs-on: self-hosted

    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v4

      - name: 🔧 Ensure namespace
        run: |
          kubectl get ns app || kubectl create ns app

      - name: 🔐 Apply/update env Secret
        env:
          BOOTSTRAP: ${{ vars.KAFKA_BOOTSTRAP_SERVERS_VALUE }}
          GROUP_ID: ${{ vars.KAFKA_GROUP_ID_MESSAGE_RESPONDER }}
          REQ_TOPIC: ${{ vars.KAFKA_TOPIC_NAME_TELEGRAM_NORMALIZED }}
          RESP_TOPIC: ${{ vars.KAFKA_TOPIC_NAME_TG_RESPONSE_PREPARER }}
          OCR_TOPIC: ${{ vars.KAFKA_TOPIC_NAME_OCR_REQUEST }}
          SASL_USER: ${{ secrets.KAFKA_SASL_USERNAME }}
          SASL_PASS: ${{ secrets.KAFKA_SASL_PASSWORD }}
          CLIENT_ID: ${{ vars.KAFKA_CLIENT_ID_MESSAGE_RESPONDER }}
        run: |
          kubectl -n app create secret generic message-responder-env \
            --from-literal=KAFKA_BOOTSTRAP_SERVERS_VALUE="${BOOTSTRAP}" \
            --from-literal=KAFKA_GROUP_ID_MESSAGE_RESPONDER="${GROUP_ID}" \
            --from-literal=KAFKA_TOPIC_NAME_TELEGRAM_NORMALIZED="${REQ_TOPIC}" \
            --from-literal=KAFKA_TOPIC_NAME_TG_RESPONSE_PREPARER="${RESP_TOPIC}" \
            --from-literal=KAFKA_TOPIC_NAME_OCR_REQUEST="${OCR_TOPIC}" \
            --from-literal=KAFKA_SASL_USERNAME="${SASL_USER}" \
            --from-literal=KAFKA_SASL_PASSWORD="${SASL_PASS}" \
            --from-literal=KAFKA_CLIENT_ID_MESSAGE_RESPONDER="${CLIENT_ID}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: 📤 Apply manifests
        run: |
          kubectl -n app apply -f k8s/message-responder/deploy.yaml

      - name: 🖼 Update image tag
        env:
          IMAGE_TAG: ${{ github.event.inputs.image_tag }}
        run: |
          TAG=${IMAGE_TAG:-latest}
          REPO=ghcr.io/pufferfish-io/message-responder
          IMAGE="${REPO}:${TAG}"
          echo "Deploying image: ${IMAGE}"
          kubectl -n app set image deployment/message-responder message-responder=${IMAGE}

      - name: 🔎 Rollout status
        run: |
          set -e
          kubectl -n app rollout status deployment/message-responder --timeout=120s
          kubectl -n app get pods -o wide

      - name: 📝 Recent logs (best effort)
        continue-on-error: true
        run: |
          kubectl -n app logs deploy/message-responder --tail=200
