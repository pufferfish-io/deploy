name: 🚀 Deploy telegram-normalizer

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (defaults to latest)"
        required: false
        default: "latest"

jobs:
  deploy:
    name: 🔄 Deploy via Helm
    runs-on: self-hosted

    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v4

      - name: 🔧 Ensure namespace
        run: |
          kubectl get ns app || kubectl create ns app

      - name: 🔐 Apply/update env Secret
        env:
          TG_TOKEN: ${{ secrets.TELEGRAMTOKEN }}
          BOOTSTRAP: ${{ vars.KAFKA_BOOTSTRAP_SERVERS_VALUE }}
          GROUP_ID: ${{ vars.KAFKA_GROUP_ID_TELEGRAM_NORMALIZER }}
          SASL_USER: ${{ secrets.KAFKA_SASL_USERNAME }}
          SASL_PASS: ${{ secrets.KAFKA_SASL_PASSWORD }}
          TOPIC_UPDATES: ${{ vars.KAFKA_TOPIC_NAME_TELEGRAM_UPDATES }}
          TOPIC_NORMALIZED: ${{ vars.KAFKA_TOPIC_NAME_NORMALIZED_MSG }}
          S3_ENDPOINT: ${{ vars.S3_ENDPOINT }}
          S3_ACCESS: ${{ secrets.S3_ACCESS_KEY }}
          S3_SECRET: ${{ secrets.S3_SECRET_KEY }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_USE_SSL: ${{ vars.S3_USE_SSL }}
        run: |
          kubectl -n app create secret generic telegram-normalizer-env \
            --from-literal=TELEGRAMTOKEN="${TG_TOKEN}" \
            --from-literal=KAFKA_BOOTSTRAP_SERVERS_VALUE="${BOOTSTRAP}" \
            --from-literal=KAFKA_GROUP_ID_TELEGRAM_NORMALIZER="${GROUP_ID}" \
            --from-literal=KAFKA_SASL_USERNAME="${SASL_USER}" \
            --from-literal=KAFKA_SASL_PASSWORD="${SASL_PASS}" \
            --from-literal=KAFKA_TOPIC_NAME_TELEGRAM_UPDATES="${TOPIC_UPDATES}" \
            --from-literal=KAFKA_TOPIC_NAME_NORMALIZED_MSG="${TOPIC_NORMALIZED}" \
            --from-literal=S3_ENDPOINT="${S3_ENDPOINT}" \
            --from-literal=S3_ACCESS_KEY="${S3_ACCESS}" \
            --from-literal=S3_SECRET_KEY="${S3_SECRET}" \
            --from-literal=S3_BUCKET="${S3_BUCKET}" \
            --from-literal=S3_USE_SSL="${S3_USE_SSL}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: 📤 Apply manifests
        run: |
          kubectl -n app apply -f k8s/telegram-normalizer/deploy.yaml

      - name: 🖼 Update image tag
        env:
          IMAGE_TAG: ${{ github.event.inputs.image_tag }}
        run: |
          TAG=${IMAGE_TAG:-latest}
          REPO=ghcr.io/pufferfish-io/telegram-normalizer
          IMAGE="${REPO}:${TAG}"
          echo "Deploying image: ${IMAGE}"
          kubectl -n app set image deployment/telegram-normalizer telegram-normalizer=${IMAGE}
      - name: 🧾 Show deployed image
        run: |
          kubectl -n app get deployment telegram-normalizer \
            -o jsonpath='{.spec.template.spec.containers[0].image}' && echo

      - name: 🔎 Rollout status
        run: |
          set -e
          kubectl -n app rollout status deployment/telegram-normalizer --timeout=120s
          kubectl -n app get pods -o wide

      - name: 📝 Recent logs (best effort)
        continue-on-error: true
        run: |
          kubectl -n app logs deploy/telegram-normalizer --tail=200
