name: 🚀 Deploy telegram-forwarder

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (defaults to latest)"
        required: false
        default: "latest"

jobs:
  deploy:
    name: 🔄 Deploy via Helm
    runs-on: self-hosted

    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v4

      - name: 🔧 Ensure namespace
        run: |
          kubectl get ns app || kubectl create ns app

      - name: 🔐 Apply/update env Secret
        env:
          SERVER_ADDR: ${{ vars.SERVER_ADDR_TELEGRAM_FORWARDER }}
          BOOTSTRAP: ${{ vars.KAFKA_BOOTSTRAP_SERVERS_VALUE }}
          TOPIC_UPDATES: ${{ vars.KAFKA_TOPIC_NAME_TELEGRAM_UPDATES }}
          SASL_USER: ${{ secrets.KAFKA_SASL_USERNAME }}
          SASL_PASS: ${{ secrets.KAFKA_SASL_PASSWORD }}
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        run: |
          kubectl -n app create secret generic telegram-forwarder-env \
            --from-literal=SERVER_ADDR_TELEGRAM_FORWARDER="${SERVER_ADDR:-:8080}" \
            --from-literal=KAFKA_BOOTSTRAP_SERVERS_VALUE="${BOOTSTRAP}" \
            --from-literal=KAFKA_TOPIC_NAME_TELEGRAM_UPDATES="${TOPIC_UPDATES}" \
            --from-literal=KAFKA_SASL_USERNAME="${SASL_USER}" \
            --from-literal=KAFKA_SASL_PASSWORD="${SASL_PASS}" \
            --from-literal=TELEGRAM_TOKEN="${TG_TOKEN}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: 📤 Apply manifests
        run: |
          kubectl -n app apply -f k8s/telegram-forwarder/deploy.yaml
      - name: ⏱️ Wait TLS certificate (telegram-forwarder)
        run: |
          echo "⏳ Waiting for certificate/tg-forwarder-tls in app namespace..."
          kubectl -n app wait --for=condition=Ready certificate/tg-forwarder-tls --timeout=10m || true
          echo "📄 Certificate status:" && kubectl -n app get certificate tg-forwarder-tls -o wide || true
          echo "🧩 ACME resources (app):" && kubectl -n app get challenge,order -o wide || true
          echo "🛠 Ingress list (app):" && kubectl -n app get ingress -o wide || true

      - name: 🖼 Update image tag
        env:
          IMAGE_TAG: ${{ github.event.inputs.image_tag }}
        run: |
          TAG=${IMAGE_TAG:-latest}
          REPO=ghcr.io/pufferfish-io/telegram-forwarder
          IMAGE="${REPO}:${TAG}"
          echo "Deploying image: ${IMAGE}"
          kubectl -n app set image deployment/telegram-forwarder telegram-forwarder=${IMAGE}
      - name: 🔎 Rollout status
        run: |
          set -e
          kubectl -n app rollout status deployment/telegram-forwarder --timeout=120s
          kubectl -n app get pods -o wide
      - name: 📝 Recent logs (best effort)
        continue-on-error: true
        run: |
          kubectl -n app logs deploy/telegram-forwarder --tail=200
