apiVersion: v1
kind: Namespace
metadata:
  name: app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-forwarder
  namespace: app
  labels:
    app: telegram-forwarder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telegram-forwarder
  template:
    metadata:
      labels:
        app: telegram-forwarder
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: telegram-forwarder
          image: ghcr.io/pufferfish-io/telegram-forwarder:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: telegram-forwarder-env
          env:
            - name: SERVER_ADDR_TELEGRAM_FORWARDER
              value: ":8080"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: telegram-forwarder
  namespace: app
  labels:
    app: telegram-forwarder
spec:
  type: ClusterIP
  selector:
    app: telegram-forwarder
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: telegram-forwarder
  namespace: app
  labels:
    app: telegram-forwarder
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-http01
    nginx.ingress.kubernetes.io/proxy-body-size: 500m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tg.forwarder.pufferfish.ru
      secretName: tg-forwarder-tls
  rules:
    - host: tg.forwarder.pufferfish.ru
      http:
        paths:
          - path: /webhook
            pathType: Prefix
            backend:
              service:
                name: telegram-forwarder
                port:
                  number: 8080
          - path: /healthz
            pathType: Prefix
            backend:
              service:
                name: telegram-forwarder
                port:
                  number: 8080
